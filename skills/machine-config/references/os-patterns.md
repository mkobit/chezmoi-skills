# OS and hostname patterns

Patterns for branching on operating system, distribution, architecture, and hostname.

## Sections (to be authored)

- `.chezmoi.os` / `.chezmoi.arch` values and common conditionals.
- Linux distribution detection via `.chezmoi.osRelease`.
- Hostname-based branching with `.chezmoi.hostname`.
- WSL detection.
- OS-specific files vs OS-specific blocks within one template.
