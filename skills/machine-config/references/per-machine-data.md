# Per-machine data

Patterns for defining and consuming machine-specific template data.

## Sections (to be authored)

- `[data]` in the config file vs `.chezmoidata.$FORMAT` in the source state.
- Prompting for machine values during `chezmoi init` with `promptString`/`promptBool`.
- Machine roles/profiles (e.g., work vs personal) as boolean data flags.
- Precedence and merging of data sources.
